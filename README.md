# Split For Editors v2
Modern dark Android MVP for multi-split video export.

Features: video preview, precision timeline, draggable split markers, millisecond display, segment list, one-tap export, stream-copy export to Movies/Split For Editors.

Quality note: stream-copy avoids intentional re-encoding, preserving encoded quality. Exact frame cuts may require re-encoding because compressed video uses keyframes.
